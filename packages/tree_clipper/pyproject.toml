[project]
name = "tree-clipper"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
bpy = [ "bpy>=5.0.0" ]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"